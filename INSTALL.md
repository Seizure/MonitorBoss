# Setup

## Windows

```bat
python -m venv .venv
.venv\Scripts\activate.bat
python -m pip install uv
uv sync
```

## Linux

```sh
python -m venv .venv
source .venv/bin/activate
python -m pip install uv
uv sync
```
